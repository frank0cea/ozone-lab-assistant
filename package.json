{
  "name": "ozone-lab-assistant",
  "version": "0.3.0",
  "private": true,
  "description": "重铬酸钾法标定硫代硫酸钠的本地计时、计算与记录助手",
  "scripts": {
    "test": "node tests/calculations.test.js",
    "check": "node --check src/calculations.js && node --check src/app.js && node --check sw.js && npm test"
  }
}
